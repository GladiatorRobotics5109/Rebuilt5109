package frc.robot.subsystems.intake;

import edu.wpi.first.wpilibj2.command.SubsystemBase;

public class IntakeSubsystem extends SubsystemBase {
}
/*public void rollersRunVoltage(double volts) - runs the intake rollers with the provided voltage
public void pivotRunVoltage(double volts)  - runs the intake pivot with the provided voltage
public void pivotRunPosition(Rotation2d position) - runs the intake pivot to the specified position using position PID on the motor controller
@Override public void periodic()  - updates the IO’s inputs and records to the log file
public void stop() - runs the intake pivot and intake rollers at 0 volts
public Rotation2d getPivotPosition() - returns the position of the intake pivot. (0 radians = intake is pointing straight up. pi/2 radians = intake is horizontal and is deployed */